---
layout: default
---

# 《重学数学》统一写作规范

本规范用于统一各章的内容组织、数学陈述、证明深度与交付标准。它约束的是论证质量，而不要求所有章节使用完全相同的叙事节奏。章节可以保留各自的主题特点，但必须让读者能够明确回答：研究对象是什么、问题是什么、假设是什么、结论如何得到、条件删去后会怎样。

本项目面向已经学过高等数学、线性代数、概率论与统计、矩阵论和随机过程，并具备基础范畴论知识的计算机相关专业读者。可以使用这些知识作为直觉来源，但不能把跨章节依赖或非初等结论隐藏在“显然”之中。

## 1. 总体原则

每章必须完成三件事：

1. 提出一个边界清楚、可以被数学化的问题；
2. 从具体困难和典型例子引出必要的抽象概念；
3. 建立一条可逐步检查的“定义—引理—定理—证明—应用”推理链。

直觉解释用于说明严格内容为何合理，不能代替定义或证明。形式推导、数值证据和图像观察可以提供动机，但必须明确标注其逻辑地位。

## 2. 一章的标准结构

标题可以因主题调整，但正文必须覆盖下列模块。

| 模块 | 必须回答的问题 |
| --- | --- |
| 阅读说明 | 主问题、预备知识、核心结果和重要符号是什么？ |
| 问题与动机 | 旧工具在哪里失效？本章具体要解决什么？ |
| 数学对象 | 底层集合、空间、映射、运算和等价关系是什么？ |
| 核心定义 | 定义为何这样写？每个条件承担什么作用？ |
| 核心定理 | 假设、量词和结论分别是什么？ |
| 完整证明 | 每一步依据什么？关键假设在哪里使用？ |
| 例子与反例 | 定义如何工作？删去条件后哪里失败？ |
| 应用与连接 | 结果如何回答开篇问题？依赖或支撑哪些章节？ |
| 总结 | 本章建立了怎样的推理链？ |
| 参考资料 | 非初等结论、历史事实和版本差异来自哪里？ |

建议每章选取 2–5 个真正支撑主线的核心结果。非核心材料可以用命题、引理、例子、备注或“进一步阅读”组织，避免主线被百科式罗列淹没。

## 3. 开篇先把问题定义清楚

开篇不得只写“本章介绍某理论”。至少说明：

- 输入对象是什么；
- 希望构造、计算、分类或证明什么；
- 已知条件是什么；
- 旧方法的具体障碍是什么；
- 什么结果算作解决了问题；
- 本章暂不讨论哪些情形。

若问题来自物理、统计或计算机科学，还应区分原始业务或物理描述与数学模型，并说明建模假设。例如，把连续介质近似、独立同分布、有限维、光滑性或可计算性写成明确条件，而不是默认事实。

## 4. 定义的写法

正式定义必须说明：

1. 对象所属的集合、空间、域或范畴；
2. 所有必要假设；
3. 映射的定义域和值域；
4. 条件中的量词；
5. 等价关系、商对象或“几乎处处”等识别方式；
6. 定义是否依赖基、坐标、代表元或其他选择。

推荐格式：

> **定义（名称）**  
> 设【对象及假设】。若对任意【量词范围】满足【条件】，则称【对象】为【术语】。

定义之后原则上依次给出：

- 动机：这个条件排除了什么困难；
- 最小非平凡例子：可以直接验证；
- 非例或反例：只满足部分条件；
- 边界情形：空集、零对象、退化参数、低维或离散情形；
- 相近概念辨析：说明包含关系、等价条件或差异。

例如，不能只写“(f) 是可测函数，如果阈值事件可测”。应明确
[
f:(Omega,mathcal F)longrightarrow(mathbb R,mathcal B(mathbb R)),
]
并写出“对所有 (ainmathbb R)”以及所用原像集合；若声称以 (>)、(ge) 或开集原像定义等价，必须证明或引用该等价性。

## 5. 定理的陈述

定理、命题和引理都必须可以脱离上下文独立阅读。陈述中需要包含：

- 对象类型与所在空间；
- 全部假设及量词；
- 参数范围；
- 精确结论；
- 结论成立的意义，例如逐点、几乎处处、依概率、依分布或 (L^p)；
- 存在性与唯一性各自的范围；
- 版本依赖或约定差异。

不要把必要条件写成充分条件，不要用等号代替近似、同构、渐近或分布相等。若一个结果只有在有限维、紧致、完备、可分、光滑或有界等条件下成立，必须在陈述中出现这些条件。

## 6. “完整证明”的标准

本项目的核心定理要求完整证明。完整证明是指：在已经明确列出的前置结果基础上，读者可以逐步验证结论，不需要自行补上决定成败的论证。

### 6.1 证明结构

每个核心证明至少包含：

1. **证明入口**：说明策略、关键构造或归纳参数；
2. **条件检查**：确认将使用的定理或引理满足其假设；
3. **逐步推理**：每个非平凡等式、蕴含和估计都有依据；
4. **分支处理**：必要时分别处理存在与唯一、充分与必要、一般与边界情形；
5. **结论回收**：明确指出已得到原命题的全部结论。

允许省略纯机械代数计算，但必须说明计算目标和关键恒等式；只要省略会掩盖思想、条件或易错点，就应展开。

### 6.2 必须解释的操作

出现下列操作时，应给出合法性依据：

- 交换极限、积分、求和、期望或微分；
- 从局部结论推出全局结论；
- 选择子列、代表元、基或坐标；
- 使用选择公理、紧致性、完备性或可分性；
- 对无穷级数逐项运算；
- 在商空间中定义映射；
- 对随机变量修改零测集；
- 把形式解认作经典解、弱解或分布解；
- 把数值实验或图像现象提升为一般结论。

常见依据应明确写出，例如控制收敛定理、单调收敛定理、Fubini–Tonelli 定理、一致收敛、紧致性定理或前章已证明的结果。

### 6.3 外部结果与前置结果

核心步骤不能整体交给一个未解释的外部定理。依赖重大结果时，采用以下一种方式：

- 该结果已在前章完整证明，并给出准确链接；
- 本章先证明所需引理；
- 将其明确列为前置定理，完整陈述其假设与结论，并说明本章的证明相对于该前置结果是完整的。

若某个结果超出本项目当前层级而不展开证明，应明确标为“引用定理”或“进一步阅读”，不得把它列作本章已经完整证明的核心定理。

### 6.4 直觉与形式计算的边界

使用 Dirac (delta)、形式幂级数、路径积分、无穷维“体积元”或其他启发式对象时，必须标明：

- 当前表达是严格对象、分布意义、形式记号还是物理启发；
- 哪些运算在何种条件下合法；
- 严格版本需要怎样的函数空间或极限过程。

“显然”“容易验证”“类似可得”只用于一步即可核对的内容；否则必须补出推导或精确引用。

## 7. 例子、反例与边界

每个核心概念至少配置：

- 一个可以直接计算或验证的简单例子；
- 一个满足部分条件但不满足定义的非例；
- 一个展示边界或异常行为的例子。

每个重要定理原则上配置：

- 一个满足所有假设的应用；
- 一个违反关键假设、从而使结论失败的反例；
- 对反例失败位置的解释，而不只是给出名称。

例子不能只展示公式代入，还应说明它验证了定义的哪些条件。反例应尽量最小化，使读者能看清究竟是哪一个假设不可删除。

## 8. 符号、语言与公式

- 所有符号首次出现时定义；长期使用的符号放入开篇符号表。
- 明确标注实数域或复数域、有限维或无限维。
- 同一符号在同一章中不承担不同角色。
- 区分元素、集合、映射、算子、随机变量和等价类。
- 等号只表示真正相等；近似、渐近、同构、同胚、等价和依分布相等使用对应符号。
- “函数”与“几乎处处相等的等价类”不得混用。
- 涉及物理量时进行量纲检查，并解释无量纲化。
- 首次出现的缩写应给出全称。
- 交换图必须在正文中写出对应路径等式；图表必须解释其数学含义。
- 行间公式的 `$$` 分隔符必须各占一行，并在整个公式块前后各保留一个空行，避免公式紧贴正文、标题或列表项而被 Markdown 解析为行内内容。
- 公式应使用 GitHub Pages 当前支持的 LaTeX 命令。算子名优先写成 `\mathrm{Re}`、`\mathrm{DFT}` 等形式，不使用当前渲染器禁用的 `\operatorname` 宏。
- 集合花括号统一写成 `\lbrace ...\rbrace`。不要写 `\{...\}`，也不要把它接在 `\left`、`\right`、`\bigl`、`\bigr` 等定界符命令后；Markdown 预处理可能先吞掉花括号前的反斜杠，使数学渲染器收到非法的 `\bigl{...\bigr}`。
- 分段定义、分类结果和多步推导应拆成多个独立的 `$` 公式块，必要时在正文中重复左端表达式；不使用当前渲染链可能破坏的 `cases`、`aligned`、`gathered`、`split` 或 `array` 等多行环境，也不依赖 `&` 与 `\\` 对齐。
- 公式中不要出现紧邻的方括号与函数求值括号，例如 `H[f](t)`；Markdown 可能把 `[f](t)` 识别为链接。改写为 `H(f)(t)`、`(Hf)(t)`，或先定义中间量。
- 提交前检查并删除公式中的不间断空格等不可见 Unicode 空白；排版空白使用普通空格或明确的 LaTeX 间距命令。
- 公式验收不能只解析 Markdown 源文件中的原始 LaTeX；还要扫描 `\{`、`\}` 及其与定界符命令的组合，确认 Markdown 预处理不会改变传给数学渲染器的输入。

中文叙述应优先使用短而完整的句子。代词“它”“这”若可能指向多个对象，应改写为明确名称。不要用“某种意义下”“通常”“一般来说”掩盖条件；若确有多个版本，应指出当前采用的版本。

## 9. 依赖、链接与引用

章节应列出所需预备知识，并尽量链接到仓库中已经定义或证明该结果的位置。重复使用的结果保持同一名称和符号约定。

以下内容需要来源：

- 非初等定理及其标准版本；
- 历史人物、年份和优先权判断；
- 前沿研究和工程应用；
- 存在多个常见版本或不同假设的结论；
- 数值常数、实验结论和具体性能判断。

优先使用标准教材、专著、原始论文和权威课程资料。参考资料至少给出作者、标题、年份和相关章节或定理编号；网络资料应给出可稳定访问的链接和访问日期。正文中的关键引用应能对应到参考资料列表，不只在章末堆放书目。

## 10. 图片、代码与计算实验

图片、动画和代码用于揭示结构或验证计算，不作为严格证明的替代品。

- 图片必须有说明文字，并在正文中解释坐标、参数与结论。
- 生成图片的脚本应可重复运行，固定随机种子并记录主要依赖。
- 数值实验应写明算法、参数、误差指标和终止条件。
- 若离散结果近似连续对象，应说明收敛概念和误差来源。
- 修改图片引用前先检查相对路径，不因渲染器问题直接删除资源。

## 11. 写作与审校流程

建议按以下顺序完成一章：

1. 写出开篇问题、对象和范围；
2. 画出概念依赖与证明依赖；
3. 选择 2–5 个核心结果；
4. 补齐定义、引理和完整证明；
5. 加入例子、反例和边界情形；
6. 回到开篇问题验证是否真正解决；
7. 统一符号、链接和引用；
8. 按验收清单逐项复核。

审校时分别检查四个层次：

- **数学正确性**：陈述、计算、证明和条件；
- **逻辑完整性**：依赖、量词、推理跳步和边界；
- **教学可读性**：动机、例子、节奏和概念连接；
- **工程质量**：Markdown、公式、图片、脚本和链接。

## 12. 章节验收清单

一章只有在以下问题均可肯定回答时，才视为完成：

- [ ] 开篇问题具体、边界清楚，并说明什么算作解决；
- [ ] 预备知识和跨章依赖已经列出；
- [ ] 所有对象、符号、量词和假设已经定义；
- [ ] 每个核心定义都有动机、例子和必要的反例；
- [ ] 核心定理能够脱离上下文独立阅读；
- [ ] 核心定理具有完整证明；
- [ ] 每个关键极限、积分或求和交换都有依据；
- [ ] 存在性与唯一性、必要性与充分性分别处理；
- [ ] 严格论证、直觉解释、形式推导和数值证据已经区分；
- [ ] 删除重要假设后的失败情形已经说明；
- [ ] 结果与开篇问题及前后章节重新连接；
- [ ] 公式、术语、图片和相对链接一致；
- [ ] 历史、前沿和非初等结论有可靠来源；
- [ ] 总结能够还原本章的完整推理链。

新章节可从 [章节模板](chapter-template.md) 开始；模板是结构提醒，不应为了填满栏目而加入无关内容。
